# Gotchas

Non-obvious things that bite. One file per gotcha. File names should be greppable — pick the term you'd actually search for at 2am.

When adding a new entry, write it for **future-you who has forgotten everything**. Lead with the symptom (the thing you'd recognize), then the cause, then how to handle it. Link to the commit that fixed it if there is one.
